# Cross-application tests

Backend API tests live with the Django app in `apps/api/cases/tests.py`; worker tests live in `services/forensic-worker/tests`; frontend tests live in `apps/web/tests` and `apps/web/e2e`. This directory is reserved for cross-service contract fixtures.
