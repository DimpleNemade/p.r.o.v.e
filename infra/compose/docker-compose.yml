services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: forensic
      POSTGRES_USER: forensic
      POSTGRES_PASSWORD: forensic
    ports: ["5432:5432"]
    volumes: ["forensic-postgres:/var/lib/postgresql/data"]
  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
volumes:
  forensic-postgres:
