# Mermaid diagrams

Sources are kept as Markdown with embedded Mermaid blocks and are cross-linked from the engineering documents. Run `python scripts/validate_mermaid.py` from the repository root. SVG/PNG export is intentionally deferred until Mermaid CLI is installed; source validation remains available.
