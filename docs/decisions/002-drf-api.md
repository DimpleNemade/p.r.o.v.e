# ADR-002: Django REST Framework as API layer

Status: accepted.

DRF provides explicit serializers, permissions, API testing, and OpenAPI integration. It keeps case authorization close to the domain models and supports a browser and Tauri client.
