# ADR-009: Provenance as a first-class data model

Status: accepted.

Evidence, processing runs, artifacts, timeline events, findings, reports, and exports need durable links. A dedicated `ProvenanceLink` model allows the UI, API, and future export manifest to make traceability explicit and testable.
