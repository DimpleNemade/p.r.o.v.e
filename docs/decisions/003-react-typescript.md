# ADR-003: React and TypeScript investigator interface

Status: accepted.

React supports progressive disclosure and a shared browser/desktop UI. TypeScript catches contract drift and supports component and end-to-end testing.
