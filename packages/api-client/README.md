# API client package

The browser currently keeps a small typed client in `apps/web/src/api.ts` so the V0.1 scaffold can run without publishing a package. This directory is reserved for extracting the generated OpenAPI client once the API contract stabilizes.
